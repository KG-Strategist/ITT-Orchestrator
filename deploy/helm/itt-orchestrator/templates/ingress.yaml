apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "itt-orchestrator.fullname" . }}
  labels:
    {{- include "itt-orchestrator.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              service:
                {{- if eq .path "/api" }}
                name: {{ include "itt-orchestrator.fullname" $ }}-api
                port:
                  number: 3001
                {{- else }}
                name: {{ include "itt-orchestrator.fullname" $ }}-frontend
                port:
                  number: 3000
                {{- end }}
          {{- end }}
    {{- end }}
